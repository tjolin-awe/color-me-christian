{
  "name": "colour-me-christian",
  "version": "1.0.0",
  "description": "An online biblical art studio",
  "main": "index.js",
  "scripts": {
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && webpack --mode production",
    "watch": "set NODE_OPTIONS=--openssl-legacy-provider && webpack --watch --mode development",
    "start": "set NODE_OPTIONS=--openssl-legacy-provider && webpack --mode development && http-server -c-1"
  },
  "author": "Thomas Jolin",
  "license": "MIT",
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-loader": "^7.1.4",
    "babel-preset-env": "^1.6.1",
    "expose-loader": "^0.7.5",
    "webpack": "^4.1.1",
    "webpack-cli": "^2.0.12"
  },
  "dependencies": {
    "phaser-ce": "^2.10.2"
  }
}
